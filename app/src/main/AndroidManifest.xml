<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.github.learn.rvdatabinding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:replace="allowBackup">

        <activity
            android:name="com.github.learn.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.github.learn.rvdatabinding.RefreshRecyclerActivity"
            android:label="@string/label_action_label_recycler_view">
        </activity>

        <activity
            android:name="com.github.learn.stickyheaders.StickyHeadersActivity"
            android:label="@string/label_action_label_sticky_header_view">
        </activity>
    </application>

</manifest>
